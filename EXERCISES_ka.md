> 🇬🇧 [English version](./EXERCISES.md) · [← მიმოხილვაზე დაბრუნება](./README_ka.md) · [📤 როგორ ჩააბაროთ](./SUBMITTING_ka.md)

# საშინაო დავალება 6 — სავარჯიშოები

შექმენით ქვემოთ ჩამოთვლილი ფაილები `submissions/<your-username>/`-ის შიგნით და დაწერეთ JavaScript კოდი თითოეული დავალების ამოსახსნელად.

გამოიყენეთ მხოლოდ ის, რასაც ბოლოში მოცემული [წესები](#წესები) უშვებს. თითოეული ფაილის გაშვება შეგიძლიათ ასე:

```bash
node exercise_1.js
```

ყოველი სავარჯიშო აჩვენებს თავის **მოსალოდნელ შედეგს**. თქვენმა პროგრამამ ზუსტად ის უნდა დაბეჭდოს.

როცა მნიშვნელობას წარწერასთან ერთად ბეჭდავთ, გადაეცით წარწერა და მნიშვნელობა **ორ არგუმენტად**: `console.log("Sum:", total);` გამოიყენეთ თითოეულ მოსალოდნელ შედეგში ნაჩვენები წარწერები.

---

## `exercise_1.js` — გამოაცხადეთ ფუნქცია, გამოიძახეთ ფუნქცია

დაწერეთ ორი ფუნქცია, რომლებსაც **პარამეტრები არ აქვთ** და მხოლოდ ბეჭდავენ:

1. `sayHello()` — ბეჭდავს `Hello!`
2. `printLine()` — ბეჭდავს ოც `-` სიმბოლოს

შემდეგ, ფუნქციების ქვემოთ, გამოიძახეთ ისინი ამ თანმიმდევრობით: `sayHello`, `sayHello`, `printLine`, `sayHello`.

მოსალოდნელი შედეგი:

```text
Hello!
Hello!
--------------------
Hello!
```

მიაქციეთ ყურადღება ორ რამეს. ფუნქციის სხეული **არ** გაეშვება მაშინ, როცა მას წერთ — მხოლოდ მაშინ, როცა მას **გამოიძახებთ**. და ერთხელ დაწერილი ფუნქციის გამოძახება იმდენჯერ შეგიძლიათ, რამდენჯერაც მოგინდებათ.

> მინიშნება: `"-".repeat(20)` მოგცემთ ოც ტირეს, ასე რომ ხელით აკრეფა არ დაგჭირდებათ.

---

## `exercise_2.js` — პარამეტრები

პარამეტრი არის მნიშვნელობა, რომელსაც ფუნქციას გამოძახებისას გადასცემთ. ფუნქციის შიგნით ის ჩვეულებრივი ცვლადივით იქცევა.

დაწერეთ სამი ფუნქცია, რომლებიც ბეჭდავენ:

1. `greet(name)` — ბეჭდავს `Hello, <name>!`
2. `printAge(name, age)` — ბეჭდავს `<name> is <age> years old`
3. `printSquare(n)` — ბეჭდავს `n`-ს საკუთარ თავზე გამრავლებულს

შემდეგ გამოიძახეთ ისინი ამ თანმიმდევრობით:

```javascript
greet("Nino")
greet("Giorgi")
printAge("Mariam", 22)
printSquare(6)
printSquare(9)
```

მოსალოდნელი შედეგი:

```text
Hello, Nino!
Hello, Giorgi!
Mariam is 22 years old
36
81
```

ერთმა და იმავე `greet` ფუნქციამ ორი განსხვავებული ხაზი დაბეჭდა, რადგან ორი განსხვავებული მნიშვნელობა გადაეცით. სწორედ ეს არის პარამეტრის მთელი აზრი.

> მინიშნება: გამოიყენეთ შაბლონური სტრიქონი — `` console.log(`Hello, ${name}!`); ``

---

## `exercise_3.js` — `return` არ არის `console.log`

ეს დავალების ყველაზე მნიშვნელოვანი სავარჯიშოა. დაუთმეთ მას დრო.

გადმოწერეთ ეს ორი ფუნქცია თქვენს ფაილში:

```javascript
function printDouble(n) {
    console.log(n * 2);
}

function doubleOf(n) {
    return n * 2;
}
```

ისინი თითქმის ერთნაირად გამოიყურებიან, მაგრამ მხოლოდ ერთი მათგანი აბრუნებს **მნიშვნელობას** იმ ადგილას, საიდანაც გამოიძახეს.

ახლა დაწერეთ ეს ოთხი ხაზი, ამ თანმიმდევრობით:

1. გამოიძახეთ `printDouble(5)` ცალკე.
2. დაბეჭდეთ `doubleOf(5)`-ის შედეგი.
3. შეინახეთ `doubleOf(8)` `const`-ში სახელად `result`, შემდეგ დაბეჭდეთ `result`.
4. დაბეჭდეთ `doubleOf(5) + doubleOf(10)`.

მოსალოდნელი შედეგი:

```text
10
10
16
30
```

შემდეგ უპასუხეთ ამ კითხვას ფაილის ბოლოს, კომენტარში:

**რატომ ვერ დაწერთ `printDouble(5) + printDouble(10)`-ს ისე, რომ `30` მიიღოთ?**

> მინიშნება: `return` ზუსტად იმ ადგილას სვამს ნამდვილ მნიშვნელობას, სადაც გამოძახება დაწერეთ — მისი გაშვების შემდეგ JavaScript `doubleOf(5) + doubleOf(10)`-ს ხედავს, როგორც `10 + 20`-ს. ფუნქცია `return`-ის გარეშე უკან აბრუნებს `undefined`-ს, ხოლო `undefined + undefined` რიცხვი არაა.

---

## `exercise_4.js` — პატარა ფუნქციები, რომლებიც აბრუნებენ

დაწერეთ ოთხი ფუნქცია. თითოეული **აბრუნებს** თავის პასუხს და არცერთი არ იყენებს `console.log`-ს შიგნით:

1. `add(a, b)` — აბრუნებს ორი რიცხვის ჯამს.
2. `multiply(a, b)` — აბრუნებს ორი რიცხვის ნამრავლს.
3. `fullName(firstName, lastName)` — აბრუნებს ორ სახელს შორის ერთი ჰარით.
4. `celsiusToFahrenheit(celsius)` — აბრუნებს `celsius * 9 / 5 + 32`-ს.

შემდეგ დაბეჭდეთ, ამ თანმიმდევრობით:

```javascript
add(3, 4)
multiply(3, 4)
fullName("Nino", "Beridze")
celsiusToFahrenheit(0)
celsiusToFahrenheit(100)
add(add(1, 2), 3)
```

მოსალოდნელი შედეგი:

```text
7
12
Nino Beridze
32
212
6
```

ყურადღებით დააკვირდით ბოლო ხაზს. ჯერ `add(1, 2)` გაეშვება, `return` მას `3`-ად აქცევს და *სწორედ ეს* მიდის გარე `add`-თან პირველ პარამეტრად. **გამოძახება, რომელიც მნიშვნელობას აბრუნებს, ყველგან შეიძლება გამოიყენოთ, სადაც მნიშვნელობა შეიძლება იდგეს.**

---

## `exercise_5.js` — `return` `if`-ის შიგნით

დაწერეთ სამი ფუნქცია:

1. `isEven(n)` — აბრუნებს `true`-ს ან `false`-ს. დააბრუნეთ თავად შედარება; `if` არ გჭირდებათ.
2. `biggest(a, b)` — აბრუნებს ორი რიცხვიდან უფრო დიდს, `if` / `else`-ის გამოყენებით.
3. `passOrFail(score)` — აბრუნებს სტრიქონს `"Passed"`, როცა ქულა `70` ან მეტია, სხვა შემთხვევაში კი `"Failed"`.

დაბეჭდეთ შედეგები ამ წარწერებით:

```text
isEven(4): true
isEven(7): false
biggest(10, 3): 10
biggest(2, 8): 8
passOrFail(85): Passed
passOrFail(41): Failed
```

> მინიშნება: `n % 2 === 0` უკვე `true`-ა ან `false` — შეგიძლიათ პირდაპირ ის დააბრუნოთ, ნაცვლად იმისა, რომ დაწეროთ `if (…) { return true; } else { return false; }`.
>
> `biggest`-სა და `passOrFail`-ში `if`-ის ყოველ განშტოებას თავისი `return` სჭირდება. როგორც კი ერთი მათგანი გაეშვება, ფუნქცია იქვე ჩერდება.

---

## `exercise_6.js` — ფუნქცია ციკლით შიგნით

დაიწყეთ ამ მასივით:

```javascript
const numbers = [5, 7, 10, 11, 3];
```

დაწერეთ სამი ფუნქცია. თითოეული იღებს **მთელ მასივს** პარამეტრად და აბრუნებს ერთ რიცხვს:

1. `sum(numbers)` — აბრუნებს მასივის ყველა რიცხვის ჯამს.
2. `biggestOf(numbers)` — აბრუნებს ყველაზე დიდ რიცხვს, `Math.max`-ის გამოყენების გარეშე.
3. `average(numbers)` — აბრუნებს საშუალოს. **გამოიძახეთ თქვენივე `sum` ფუნქცია მის შიგნით** იმის ნაცვლად, რომ ციკლი მეორედ დაწეროთ.

დაბეჭდეთ:

```text
Sum: 36
Biggest: 11
Average: 7.2
Sum of [1, 2, 3]: 6
```

> მინიშნება: შაბლონი ყოველთვის ერთია — გააკეთეთ ცვლადი ციკლამდე, შეცვალეთ ის ციკლის შიგნით და დააბრუნეთ (`return`) ციკლის შემდეგ. თუ `return`-ს ციკლის შიგნით დადებთ, ფუნქცია პირველივე ელემენტზე გაჩერდება.
>
> `biggestOf`-ისთვის დაიწყეთ `numbers[0]`-ით, როგორც ჯერჯერობით ყველაზე დიდით, ზუსტად ისე, როგორც მასივების workshop-ზე გავაკეთეთ.
>
> ბოლო ხაზი აჩვენებს, რატომ სჯობს პარამეტრი გარეთ მდებარე `numbers` ცვლადზე მითითებას: ერთი და იგივე ფუნქცია მუშაობს ნებისმიერ მასივზე, რომელსაც გადასცემთ.

---

## წესები

შეგიძლიათ გამოიყენოთ:

* ოპერატორები (`+ - * / % ** += ++ === < >= && || !`), `.length`, მასივის ინდექსები, `Math`-ის მეთოდები
* `const` და `let`
* შაბლონური სტრიქონები და სტრიქონის მეთოდები, მათ შორის `.repeat()`
* `if` / `else if` / `else`
* `for`, `while` და `for...of` ციკლები
* მასივები
* **ფუნქციები დეკლარაციის სახით**, პარამეტრები და `return`

აკრძალულია **ამ დავალებაში**:

* **ისრიანი ფუნქციები** (`const f = (x) => x + 1`) და ფუნქცია-გამოსახულებები (`const f = function (x) { … }`). აქ ყოველი ფუნქცია იწერება როგორც `function name(parameters) { … }`
* `map`, `filter`, `find`, `some`, `every`, `reduce`, `forEach` და ნებისმიერი სხვა მეთოდი, რომელიც ფუნქციას იღებს არგუმენტად — ციკლი თავად დაწერეთ
* ნაგულისხმევი პარამეტრები (`function greet(name = "stranger")`)
* `prompt()` — ის Node.js-ში არ არსებობს, ამიტომ ფაილი ჩავარდება

ისრიან ფუნქციებს, callback-ებსა და `map` / `filter` / `reduce`-ს მე-5 საშინაო დავალებაში შეხვდით, ასე რომ ამ სიის ნაწილი თქვენთვის ახალი არაა. ეს კარგია — ეს დავალება განზრახ დებს ამ ხელსაწყოებს გვერდზე. ისინი ყველა ჩვეულებრივი ფუნქციის დეკლარაციასა და `return`-ზეა აშენებული, აქ კი მიზანი სწორედ ის არის, რომ ეს საძირკველი ბოლომდე გამყარდეს.

---

როცა თქვენი ექვსი ფაილი მზად იქნება, გადადით გვერდზე **[როგორ ჩააბაროთ](./SUBMITTING_ka.md)**.
